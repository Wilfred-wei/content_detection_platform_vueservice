# Third-Party Notices

The worker loads DDA source code from an operator-configured local checkout. The
DDA repository is licensed under Apache-2.0. It also loads Meta's DINOv2 source
and weights from an operator-configured pinned local cache. Deployments must
retain the upstream license and notice files and independently verify the terms
of every configured checkpoint before production promotion.

The official DDA threshold is exposed as an uncalibrated supporting signal. It
is not a production accuracy or provenance guarantee.
